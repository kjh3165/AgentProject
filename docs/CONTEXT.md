# CONTEXT

## 현재 작업
- Task 5 완료 (거래 내역 UI 카테고리 연동)
- 다음 작업 대기 중

## 현재 상태
- **Front-end**: React(Vite+TS), Tailwind CSS v4, React Router
- **Back-end**: Supabase Local (Docker), Auth, Database (RLS 적용)
- **핵심 기능**:
  - 이메일 기반 인증 및 보안 정책(RLS)
  - 거래 내역 CRUD (추가, 조회, 수정, 삭제)
  - 월별 필터링 및 대시보드 실시간 통계
  - 실시간 데이터 동기화 (Realtime)
  - **카테고리 스타일(color, icon) 정보 포함**

## 향후 과제
- [ ] 시각화 차트 도입 (Recharts 등)
- [ ] 검색 및 상세 필터링 기능
